name: Example Cluster Test

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  postgres-register:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Clojure tools
        uses: DeLaGuardo/setup-clojure@13
        with:
          lein: 2.11.2

      - name: Set up Helm
        uses: azure/setup-helm@v5.0.0
        with:
          version: v3.18.4

      - name: Install packages
        run: |
          sudo apt update
          sudo apt install -y graphviz

      - name: Cache Maven dependencies
        uses: actions/cache@v5
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-m2-${{ hashFiles('project.clj', 'examples/**/project.clj') }}
          restore-keys: |
            ${{ runner.os }}-m2-

      - name: Create kind cluster
        uses: helm/kind-action@v1.14.0
        with:
          cluster_name: jepsen
          wait: 120s

      - name: Install MetalLB
        run: |
          kubectl apply -f https://raw.githubusercontent.com/metallb/metallb/v0.14.9/config/manifests/metallb-native.yaml
          kubectl wait --namespace metallb-system \
            --for=condition=Ready pods \
            --selector=app=metallb \
            --timeout=90s

          NET=$(docker network inspect kind | jq -r '.[0].IPAM.Config[] | select(.Subnet | test("^fc00") | not) | .Subnet')
          echo "IPv4 Subnet: $NET"

          PREFIX=$(echo "$NET" | cut -d. -f1-2)
          IP_RANGE="${PREFIX}.255.200-${PREFIX}.255.250"

          cat <<EOF | kubectl apply -f -
          apiVersion: metallb.io/v1beta1
          kind: IPAddressPool
          metadata:
            name: kind-pool
            namespace: metallb-system
          spec:
            addresses:
              - $IP_RANGE
          ---
          apiVersion: metallb.io/v1beta1
          kind: L2Advertisement
          metadata:
            name: l2adv
            namespace: metallb-system
          EOF

      - name: Install local jepsen-k8s snapshot
        run: lein install

      - name: Run postgres-register example
        working-directory: examples/postgres-register
        run: |
          lein run -- test --no-ssh --time-limit 30

      - name: Dump Kubernetes state on failure
        if: failure()
        run: |
          kubectl get all --all-namespaces
          kubectl describe pods --all-namespaces
          kubectl get events --all-namespaces --sort-by=.lastTimestamp

      - name: Upload Jepsen store
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: postgres-register-store
          path: examples/postgres-register/store
          if-no-files-found: ignore
